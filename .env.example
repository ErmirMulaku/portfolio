# Canonical production URL — used for Open Graph, canonical, sitemap, robots.
# Set this to your deployed domain (also configure it in Vercel project settings).
NEXT_PUBLIC_SITE_URL=https://www.ermir-mulaku.com

# Optional: enable privacy-friendly Vercel Analytics. Off unless set to "true".
NEXT_PUBLIC_ENABLE_ANALYTICS=false
